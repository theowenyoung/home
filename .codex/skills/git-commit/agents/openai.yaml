interface:
  display_name: "Git Commit"
  short_description: "Summarize current changes and commit them"
  default_prompt: "Use $git-commit to summarize the current working tree and create a straightforward Git commit."
